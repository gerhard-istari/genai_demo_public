from istari.openapi_client.models.job_status import JobStatus
from istari import Client, Configuration
from shared.constants import CAD_MODEL_ID, PARAM_FILE_NAME

print('Starting 3DX model extraction ...')
#job = client.add_job(model_id = CAD_MODEL_ID,
#                     function = '@istari:extract_parameters',
#                     tool_name = 'dassault_3dexperience')
#print(f"Job submitted with ID: {job.id}")
